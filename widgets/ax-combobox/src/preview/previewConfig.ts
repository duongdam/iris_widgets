import { DIMENSION_MOCK_DATA, createMockComboboxDatasource } from "@iris/form-core";

export const PREVIEW_PLACEHOLDER = "Select options";

export const previewMockDatasource = createMockComboboxDatasource(DIMENSION_MOCK_DATA.Site);
